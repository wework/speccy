openapi: 3.0.0
info:
  version: '1.0'
  title: Demo API
paths:
  /health/all.json:
    get:
      responses:
        '200':
          description: OK
          content:
            '*/*':
              schema:
                $ref: '#/components/schemas/health'
components:
  schemas:
    health:
      type: object
      properties:
        default:
          title: Check
          type: object
          properties:
            message:
              type: string
              description: bla
        database:
          title: Check
          type: object
          properties:
            message:
              type: string
              description: bla
