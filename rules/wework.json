{
  "require": "strict",
  "rules": [
    {
      "name": "short-summary",
      "object": "operation",
      "enabled": true,
      "description": "summary should be short (description can be long)",
      "maxLength": {
        "property": "summary",
        "value": 20
      }
    }
  ]
}
