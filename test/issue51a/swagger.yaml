swagger: 2.0
info:
  version: '1.0'
  title: Demo API

paths:

  /health/all.json:
    get:
      responses:
        200:
          description: OK
          schema:
            $ref: ../include/health.yml#/Result
